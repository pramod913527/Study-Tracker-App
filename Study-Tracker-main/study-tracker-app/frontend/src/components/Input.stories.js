import React from 'react';
import { Input } from './Input';

export default {
  title: 'Design System/Input',
  component: Input,
};

export const Basic = () => <Input label="Name" placeholder="Enter your name" />;
